image:
  file: docker/Dockerfile
tasks:
  - init: . .bashrc
  - command: cd ${HOME}/nucleus && bazel build -c opt ${COPT_FLAGS} nucleus/examples:all
github:
  prebuilds:
    master: true
    branches: true  
vscode:
  extensions:
    - jebbs.plantuml@2.13.9:Kh1501AVTKuUtjU0w5lOEQ==
